/* ============================================
   PDF SERVICE - Stub module for future PDF generation
   ============================================ */
const PdfService = (() => {
  function generatePDF(invoice, lang = 'ko') {
    console.log('PdfService.generatePDF - Not yet implemented');
  }

  return { generatePDF };
})();
